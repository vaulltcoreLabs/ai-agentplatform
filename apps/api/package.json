{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --tsconfig tsconfig.json src/index.ts",
    "build": "tsc --noEmit && bun build src/index.ts --outdir=dist --format=esm",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@vaulltcore/agent": "workspace:*",
    "@vaulltcore/sandbox": "workspace:*",
    "@vaulltcore/shared": "workspace:*",
    "ai": "catalog:",
    "better-auth": "^1.6.5",
    "hono": "^4.9.8",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vaulltcore/tsconfig": "workspace:*",
    "typescript": "^5"
  }
}
